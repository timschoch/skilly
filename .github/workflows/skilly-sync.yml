name: skilly sync
on:
  schedule: [{ cron: "30 3 * * *" }]
  workflow_dispatch: {}
  pull_request: {}
permissions:
  contents: write
  pull-requests: write
jobs:
  skilly:
    uses: timschoch/skilly/.github/workflows/sync.yml@main
    secrets: inherit
