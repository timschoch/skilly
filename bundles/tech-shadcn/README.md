# tech-shadcn

Official `shadcn` skill from shadcn-ui/ui (CLI usage, registries, presets, component composition).

## Left out (2026-08-31)

- `migrate-radix-to-base` (shadcn-ui/ui) — one-time Radix→Base UI migration helper, run ad hoc when migrating, not carried in every repo.
