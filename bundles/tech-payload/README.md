# tech-payload

Official `payload` + hub `payload-cli` + hub `payload-admin-components` (moved from colin's `.agents/skills/`, 2026-08-31 — colin should delete its local copy and consume via skilly).

Left out (2026-08-31):

- `cms-migration` (official) — we don't migrate foreign CMSes to Payload.
