# tech-lemy

Ships `lemy-write` and `lemy-setup` from `timschoch/lemy`. Left out: `lemy-brand` — brand management is rarely needed; `tech-lemy-manage` includes this bundle and adds it. `lemy-write` is THE writing skill everywhere — no third-party copywriting skills anywhere in the hub.
