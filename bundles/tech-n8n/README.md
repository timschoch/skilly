# tech-n8n

Official `n8n-io/skills` set (`-official` names only) + two czlonkowski add-ons. Needs n8n >=2.2.0 with instance-level MCP — running on our servers.

15 skills: recorded exception to the 3-4 tech-* cap — loads only in n8n repos, where nothing else competes for context.

Left out (2026-08-31):

- `n8n-io/skills` community-forked dupes (same names without `-official`) — lower installs, official wins.
- `czlonkowski/n8n-skills` other 12 skills — overlap the official set, need the community n8n-mcp server.
- `WASD-Team/n8n-claude-skill` — no registry presence, one-commit history.
- `haunchen/n8n-skills-pack` — static node reference only; add if an offline lookup is ever wanted.
