# tech-vercel

Hub `vercel-cli` only (read-ops: env, logs, inspect — deploys go through git/CI).

Left out (2026-08-31):

- `web-design-guidelines` — moved to tech-react, design belongs with the UI stack.
- `deploy-to-vercel` (vercel-labs) — teaches hand deploys and first-time linking; contradicts our git/CI + release-please flow.
- `vercel-optimize` — situational performance pass.
- `vercel-cli-with-tokens` — overlaps hub `vercel-cli`.
