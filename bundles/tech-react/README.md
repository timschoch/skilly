# tech-react

Three `vercel-labs/agent-skills` skills (incl. `web-design-guidelines`, moved here from tech-vercel — design belongs with the UI stack). Keep minimal.

Left out (2026-08-31):

- `vercel-react-view-transitions` — niche API, add when a project uses it.
- `react-native-skills` — no RN projects.
- `writing-guidelines` — writing is lemy's job.
- `deploy-to-vercel`, `vercel-cli-with-tokens`, `vercel-optimize` — tech-vercel territory.
