# tech-neon

Official `neondatabase/agent-skills` core + hub `neon-cli`. Keep minimal.

Left out (2026-08-31):

- `neon-ai-gateway`, `neon-functions`, `neon-object-storage` — Neon add-on products we don't use.
- `neon-postgres-egress-optimizer` — break-glass debugging; add if an egress bill ever spikes.
